<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="co.tiagoaguiar.netflixremake">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:name="br.com.jizreelguimaras.netflixremake.AppApplication"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.NetflixRemake">
        <activity
                android:exported="true"
                android:name="br.com.jizreelguimaras.netflixremake.ui.MovieActivity"/>
        <activity
                android:exported="true"
                android:name="br.com.jizreelguimaras.netflixremake.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>